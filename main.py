# import sqlite3
# import flask


# I somehow have to ensure that no two urls are the same
# (preferably in an efficient manner)
# (i.e. not doing rng in a while-loop until it works)
# (probably store the used ones somewhere)
def generate_link_name(number, adjective_index, noun_index):
    # All three parameters are positive integers
    # "number" should be greater than 1 (because plural nouns are used)
    pass


if __name__ == '__main__':
    print("Hello, overthere!")

